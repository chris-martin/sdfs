sdfs {

    port = 8883
    protocol = TLS

    server-store-path = "../server-store"
    client-store-path = "../client-store"

    keystore {
        ca {
            file = "../pki/ca-certs.jks"
            store-password = "changeit"
        }
        personal {
            file = "../pki/localhost.jks"
            store-password = "changeit"
            key-password = "keypass"
        }
    }

}
