sdfs {

    port = 8443

    # directory to search for .cer files
    cert-path = "../pki/"

    # the CN to use as yourself
    identity {

        # when acting as a client
        client = client

        # when acting as a server
        server = localhost
    }

    # mapping from CN to cert password
    password {

        client = asdfgh

        server = asdfgh
    }

}
